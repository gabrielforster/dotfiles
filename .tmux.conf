set -g status-style 'bg=#333333 fg=#5eacd3'
set -g mouse on
set -g base-index 1

bind-key -r f run-shell "tmux neww ~/.local/bin/tmux-sessionizer"
bind-key -r S run-shell "~/.config/scripts/tmux-sessionizer
